// UTF-8 prelexical analyzer.

#include "Global.h"
#include "Utf8.h"

/*Utf8Stream::Utf8Stream(Stream *stream) {
	_stream = stream;
}

Utf8Stream::Utf8Stream(Reader *reader) {
	_reader = reader;
}

Utf8Stream::Utf8Stream(Writer *writer) {
	_writer = writer;
}

void Utf8Stream::ReadChar(Writer *destination) {
	//_reader->ReadChar(destination);
}

void Utf8Stream::WriteChar(Reader *source) {
	//_writer->WriteChar(source);
}*/

#pragma once

#ifndef UTF8_H
#define UTF8_H

/*class Utf8Stream: public CharReader, public CharWriter {
public:
	class Utf8CharReader: public CharReader {
	};

	class Utf8CharWriter: public CharWriter {
	};

private:

	Stream *_stream;
	Reader *_reader;
	Writer *_writer;

public:

	// Здесь еще возможны варианты с Seeker'ом, но очень затратно это описывать вручную.
	Utf8Stream(Stream *stream);
	Utf8Stream(Reader *reader);
	Utf8Stream(Writer *writer);

	virtual void ReadChar(Writer *destination);
	virtual void WriteChar(Reader *source);

};*/

#endif

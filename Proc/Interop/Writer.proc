// Интерфейс последовательной записи в поток состояний.
interface Proc.Interop.Writer

	// Запись единицы данных в текущую ленту.
	void Write(var value)

	// Запись единицы данных из ленты.
	// @return Размер записанных данных. Зачем, если пишем единицу?
	var Write(var source)

	// Запись единицы данных из потока.
	// @return Размер записанных данных. Зачем, если пишем единицу?
	var Write(Reader source)
